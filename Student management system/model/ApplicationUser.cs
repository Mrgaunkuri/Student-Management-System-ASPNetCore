using Microsoft.AspNetCore.Identity;

namespace Student_management_system.model
{
    // Simple ApplicationUser that extends IdentityUser so the project can use a named user type
    public class ApplicationUser : IdentityUser
    {
        // Add additional properties here if you need to extend the user profile.
    }
}
